# 외부 검토 반영 보고서

- 대상 커밋: `1acfefe` (main)
- 검토 성격: 공개 저장소 정적 검토(실행 테스트 아님)
- 본 보고서: 각 지적을 **실제 저장소 파일로 검증**한 뒤, 이번에 반영한 것/보류한 것/사용자 결정이 필요한 것을 구분해 정리.

## 0. 검토 지적에 대한 정정(과장·오류 바로잡기)

| 검토 주장 | 실제 | 판단 |
|-----------|------|------|
| `.claude/settings.json` 에 "광범위한 `rm -rf` 권한" | 와일드카드가 아니라 **단일 정규화 경로**(`.../scratchpad/fromhwp`) 한 건 | 과장. 그래도 개인경로·세션 UUID 노출이라 제거 대상 |
| 실제 과제 내용이 "공란이라 사업성 평가 불가" | 사실이며, 원본 HWP는 **값이 없는 KEIT 빈 서식** | 정확 |
| 임베드 페이로드로 원문 노출 위험 | 복원은 가능하나 **원본이 빈 템플릿**이라 PII·기관정보·예산·서명 **전무**(디코드한 HWP 메타데이터도 클린) | 위험도 낮음(단, 채워진 문서 커밋 시 위험) |

즉 실제 개인정보 노출은 `.claude/settings.json` 의 사용자명·AppData 경로·세션 UUID **한 곳뿐** 이었다.

## 1. 즉시(P0) — 이번에 반영

| # | 지적 | 검증 | 조치 |
|---|------|------|------|
| P0-3 | settings.json 개인경로/세션 UUID 노출 | TRUE (`settings.json:4-5`) | ✅ 개인경로·UUID 제거, 경로 없는 공유용 권한(`python tools/*`, `hwp5proc:*`)으로 교체 |
| P0-3 | `settings.local.json` 이 Git 추적됨 | TRUE | ✅ `git rm --cached` 로 추적 해제(디스크엔 보존) |
| P0-3 | `.gitignore` 가 로컬 설정 미제외 | TRUE | ✅ `.claude/settings.local.json`, `.claude/settings.*.local.json`, `.claude/tmp/`, `.claude/cache/` 추가 |
| P0-2 | "수정 HWP 생성"이 미구현/수동인지 불명확 | TRUE (`hwpmd_tool.py` = export/validate/restore-original) | ✅ README에 **기능 상태표** + "최종 HWP 산출(수동/외부)" 명시 |
| P0-2 | README가 범위를 넓게 보이게 함 | TRUE | ✅ 첫 문장·"한다/하지 않는다"·흐름도·보안 경고 추가, 빈 템플릿임을 명시 |
| P1-1 | KETEP+별첨8 vs KEIT+별첨9 불일치 | TRUE (`skill_hwp_style.md:10,12` = KETEP/8 ↔ 실제 HWP=KEIT, 파일계=별첨9) | ✅ `skill_hwp_style.md` 원본 서식 설명을 KEIT·별첨9로 정정(프리셋 라벨 정리는 보류 표기) |

## 2. 중요(P1) — 검증 완료, 후속 과제로 보류

아래는 모두 **TRUE로 확인**했으나, 규모가 크거나 사용자 결정이 필요해 이번 정리에는 포함하지 않았다. 추적용으로 기록.

| # | 지적 | 검증 근거 | 후속 방향 |
|---|------|-----------|-----------|
| P0-1 | 실제 사업 내용 공란 | `00_요약문.md` 등 전부 라벨/플레이스홀더 | 실제 공고·RFP·project_brief 입력 필요(사용자 데이터) |
| P0-4 | 규정 감사 결과가 본문 미반영 | 감사문 "읽기 전용"; 문제 링크가 본문 및 `split_hwpmd_chapters.py:675-682` 에 잔존 | 불일치를 Issue/`regulation_exceptions.yaml`로 전환, 링크 수정은 규정 재확인 필요 |
| P1-2 | 검증기 하드코딩(앵커 0..804, 도메인 allowlist, 본문해시 warning) | `validate_split_hwpmd.py:122,91-100,41-42` | 매니페스트 기반 검증 + pristine/edited 모드 분리 |
| P1-3 | 입력 도구 정규식 취약(속성순서·단일라인·P00 셀 가정) | `input_template.py:35-41,131,216` | 장기: AST 기반, 단기: golden/property 테스트 |
| P1-4 | 입력 도구 비원자적, 롤백 없음 | `input_template.py` `apply`에 `--force`만 존재 | `--dry-run/--backup/--atomic/--rollback` 추가 |
| P1-5 | 로직이 `/tools`·`/.claude/skills`·`/.agents/skills` 분산 | `git ls-files` 확인 | `src/hwpmd/` 패키지 통합 + 스킬은 얇은 어댑터 |
| P1-6 | pyproject/LICENSE/CI/tests 부재 | 루트에 모두 없음 | `pyproject.toml`·의존성 문서·GitHub Actions·fixture·LICENSE |

## 3. 사용자 결정이 필요한 항목

1. **히스토리 스크럽 여부**: 정정한 `settings.json` 의 이전 값(사용자명·AppData·세션 UUID)은 여전히 커밋 히스토리(`1acfefe`)에 남는다. 민감도는 낮으나(자격증명 아님, GitHub 계정명은 이미 공개), 완전 제거를 원하면 `git filter-repo` + 강제푸시가 필요하다(파괴적: 기존 클론/포크 무효화). — 기본값: 스크럽하지 않음.
2. **저장소 공개 유지 vs 비공개**: 내용이 빈 템플릿이라 공개 유지도 방어 가능. 단, **실제 값이 채워진 문서를 이 파이프라인으로 커밋할 계획이면** 비공개 전환 또는 페이로드 임베드 배제가 안전.
3. **라이선스 선택**: 코드·원본 양식·참고계획서의 배포 권한 명시 필요(라이선스는 사용자가 선택).
4. **프리셋 라벨 정리**: `skill_hwp_style.md` 의 "KETEP양식" 프리셋 명칭을 KEIT로 개명할지(스킬 선택 로직 영향 검토 필요).

## 4. 검증 방법 메모

본 보고서의 모든 "TRUE/PARTIAL"은 커밋 `1acfefe` 기준 실제 파일·라인 및 `git ls-files` 대조로 확인했다. 전수 누출 스캔(사용자명·경로·이메일·전화번호) 결과 노출은 `settings.json` 2줄뿐이었고, 두 마스터 `.md`·장별 파일·디코드한 HWP 메타데이터에는 개인정보가 없었다.
